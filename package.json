{
  "name": "cbs-frete-calculator",
  "version": "1.0.0",
  "description": "Plataforma de cálculo de frete para transporte - Frontend + API Vercel",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "npm run build:frontend && node -e \"const fs=require('fs'); const path=require('path'); fs.cpSync('./frontend/dist', './public', {recursive:true});\"",
    "build:frontend": "cd frontend && npm install && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
